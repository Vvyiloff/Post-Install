# 📋 Руководство по настройке репозитория

Этот файл содержит инструкции по подготовке проекта к публикации на GitHub.

## ✅ Что было сделано

### 📁 Структура проекта
- ✅ Создан корневой `.gitignore` для исключения ненужных файлов
- ✅ Обновлен главный `README.md` с полной документацией
- ✅ Создана структура документации

### 📚 Документация
- ✅ `README.md` - Главная документация проекта
- ✅ `RELEASE.md` - Инструкция по созданию релизов
- ✅ `CHANGELOG.md` - История изменений
- ✅ `CONTRIBUTING.md` - Руководство для контрибьюторов
- ✅ `QUICK_START.md` - Быстрый старт
- ✅ `SETUP_GUIDE.md` - Этот файл

### 🔧 Конфигурация
- ✅ Обновлен `electron-installer/package.json` с информацией о репозитории
- ✅ Создан GitHub Actions workflow для автоматических релизов

## 🚀 Следующие шаги

### 1. Инициализация Git (если еще не сделано)

```bash
# Если репозиторий еще не инициализирован
git init
git add .
git commit -m "Initial commit: Post-Install project setup"
```

### 2. Подключение к GitHub

```bash
# Добавьте remote репозиторий
git remote add origin https://github.com/Vvyiloff/Post-Install.git

# Или если репозиторий уже существует
git remote set-url origin https://github.com/Vvyiloff/Post-Install.git
```

### 3. Первый push

```bash
# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

### 4. Очистка ненужных файлов

Перед первым коммитом убедитесь, что все билд-файлы исключены:

```bash
# Удалите временные файлы (если они не в .gitignore)
# dist/, releases/, node_modules/ и т.д. должны игнорироваться
```

## 📦 Создание первого релиза

### Автоматический способ (рекомендуется)

1. **Обновите версию:**
   ```bash
   # В electron-installer/package.json измените version на "1.0.1"
   ```

2. **Создайте тег:**
   ```bash
   git add .
   git commit -m "Prepare release v1.0.1"
   git tag -a v1.0.1 -m "Release version 1.0.1"
   git push origin main
   git push origin v1.0.1
   ```

3. **GitHub Actions автоматически:**
   - Соберет Electron приложение
   - Создаст Release
   - Загрузит установщик

### Ручной способ

1. **Соберите приложение:**
   ```bash
   cd electron-installer
   npm install
   npm run build
   ```

2. **Создайте Release на GitHub:**
   - Перейдите в Releases → Draft a new release
   - Выберите тег или создайте новый
   - Загрузите файл из `electron-installer/releases/`
   - Заполните описание
   - Опубликуйте

## 📝 Рекомендации

### Перед коммитом проверьте:

- [ ] Все изменения закоммичены
- [ ] `.gitignore` настроен правильно
- [ ] Версия обновлена в `package.json`
- [ ] Документация актуальна
- [ ] Код протестирован

### Структура коммитов:

Используйте понятные сообщения:
- `feat: добавлена новая функция`
- `fix: исправлена ошибка`
- `docs: обновлена документация`
- `refactor: рефакторинг кода`

## 🔍 Проверка перед публикацией

```bash
# Проверьте статус
git status

# Проверьте что будет закоммичено
git diff --cached

# Проверьте размер репозитория (не должно быть больших файлов)
git count-objects -vH
```

## 📚 Полезные ссылки

- [GitHub Docs](https://docs.github.com/)
- [Semantic Versioning](https://semver.org/)
- [Keep a Changelog](https://keepachangelog.com/)

## ❓ Частые вопросы

**Q: Что делать если забыл что-то в .gitignore?**
A: Удалите файлы из индекса: `git rm --cached файл`, затем добавьте в .gitignore

**Q: Как обновить существующий релиз?**
A: Создайте новый релиз с новой версией. Старые релизы не редактируются.

**Q: GitHub Actions не работает?**
A: Проверьте:
- Файл `.github/workflows/release.yml` существует
- Тег создан правильно (формат `v*`)
- Права доступа к репозиторию

---

**Готово! Теперь ваш проект готов к публикации на GitHub!** 🎉

