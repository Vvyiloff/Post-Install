# 🔄 Миграция проекта

Этот файл описывает изменения в структуре проекта.

## ✅ Что было сделано

### 📁 Новая структура папок

```
Post-Install/
├── shared/              # Общие файлы
│   └── packages.json   # Список программ (общий для обеих версий)
│
├── python/              # Python версия
│   ├── software_installer.py
│   └── README.md
│
└── electron/           # Electron версия
    ├── main.js
    ├── preload.js
    ├── package.json
    ├── src/
    └── README.md
```

### 🔧 Обновления

1. **Python версия:**
   - Файл перемещен в `python/software_installer.py`
   - Обновлены пути к `shared/packages.json`
   - Обновлен путь к лог-файлу

2. **Electron версия:**
   - Папка переименована из `electron-installer/` в `electron/`
   - Добавлена загрузка программ из `shared/packages.json`
   - Обновлены все пути

3. **GitHub Actions:**
   - Создает два отдельных архива:
     - `Post-Install-Python-vX.X.X.zip`
     - `Post-Install-Electron-vX.X.X.zip` и `Post-Install-Electron-vX.X.X-Setup.exe`

## 🗑️ Удалить после миграции

Эти файлы/папки можно удалить (они в .gitignore):

- `electron-installer/` (старая папка)
- `software_installer.py` (в корне, перемещен в python/)
- `packages.json` (в корне, перемещен в shared/)
- `dist/` (старые билды)
- `__pycache__/` (кэш Python)

## 🚀 Создание релиза

Используйте скрипт `prepare-release.ps1`:

```powershell
.\prepare-release.ps1 -Version "1.0.2"
```

Или вручную:

```bash
# 1. Обновите версию в electron/package.json
# 2. Создайте тег
git tag -a v1.0.2 -m "Release v1.0.2"
git push origin main
git push origin v1.0.2
```

GitHub Actions автоматически создаст релиз с двумя архивами!

