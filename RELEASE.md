# 📦 Инструкция по созданию релиза

Это руководство поможет вам создать и опубликовать новый релиз проекта Post-Install.

## 🚀 Быстрый старт

### 1. Подготовка

Убедитесь, что все изменения закоммичены:

```bash
git status
git add .
git commit -m "Описание изменений"
git push
```

### 2. Обновление версии

#### Electron версия:
Отредактируйте `electron-installer/package.json`:
```json
{
  "version": "1.0.2"  // Обновите версию
}
```

#### Python версия:
Обновите версию в комментариях `software_installer.py` (если есть)

### 3. Создание тега

```bash
# Создайте тег с версией
git tag -a v1.0.2 -m "Release version 1.0.2"

# Отправьте тег на GitHub
git push origin v1.0.2
```

### 4. Сборка приложений

#### Electron версия:

```bash
cd electron-installer

# Установка зависимостей (если нужно)
npm install

# Сборка приложения
npm run build
```

**Результат:** Файлы будут в `electron-installer/releases/`
- `Установщик программ v1.0.2 Setup.exe` - Установщик
- `Установщик программ-win32-x64/` - Портативная версия

#### Python версия (опционально):

```bash
# Используйте PyInstaller
pyinstaller --onefile --windowed --name "Software Installer" software_installer.py
```

**Результат:** Файл будет в `dist/software_installer.exe`

### 5. Создание Release на GitHub

1. Перейдите на страницу репозитория
2. Нажмите **Releases** → **Draft a new release**
3. Выберите созданный тег (например, `v1.0.2`)
4. Заполните форму:

   **Title:**
   ```
   Release v1.0.2
   ```

   **Description:**
   ```markdown
   ## 🎉 Что нового в v1.0.2
   
   ### ✨ Новые возможности
   - Добавлена поддержка новых программ
   - Улучшен интерфейс
   
   ### 🐛 Исправления
   - Исправлены кнопки управления окном
   - Улучшена производительность
   
   ### 📦 Скачать
   - **Electron (GUI):** [Установщик программ v1.0.2 Setup.exe](ссылка)
   - **Python (Console):** [software_installer.exe](ссылка)
   
   ### 📋 Полный список изменений
   См. [Commits](https://github.com/Vvyiloff/Post-Install/compare/v1.0.1...v1.0.2)
   ```

5. **Загрузите файлы:**
   - Перетащите `electron-installer/releases/Установщик программ v1.0.2 Setup.exe`
   - (Опционально) Перетащите `dist/software_installer.exe`

6. Нажмите **Publish release**

## 📝 Шаблон описания релиза

```markdown
## 🎉 Release v1.0.2

### ✨ Новые возможности
- Добавлена поддержка новых программ
- Улучшен интерфейс установки

### 🐛 Исправления ошибок
- Исправлены кнопки управления окном
- Улучшена стабильность при установке

### 🔧 Технические изменения
- Обновлены зависимости
- Оптимизирован код

### 📦 Скачать
- **Electron (GUI):** [Скачать установщик](ссылка)
- **Python (Console):** [Скачать exe](ссылка)

### 📋 Полный список изменений
См. [Commits](https://github.com/Vvyiloff/Post-Install/compare/v1.0.1...v1.0.2)

---

**Спасибо за использование Post-Install!** ⭐
```

## 🔄 Автоматизация (опционально)

Можно настроить GitHub Actions для автоматической сборки при создании тега. Создайте файл `.github/workflows/release.yml`:

```yaml
name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: |
          cd electron-installer
          npm install
      
      - name: Build Electron app
        run: |
          cd electron-installer
          npm run build
      
      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: installer
          path: electron-installer/releases/*.exe
```

## ✅ Чеклист перед релизом

- [ ] Все изменения закоммичены и запушены
- [ ] Версия обновлена в `package.json`
- [ ] Тесты пройдены (если есть)
- [ ] Приложение собрано и протестировано
- [ ] README.md обновлен (если нужно)
- [ ] Создан тег
- [ ] Release создан на GitHub
- [ ] Файлы загружены в Release

## 🎯 Семантическое версионирование

Используйте [Semantic Versioning](https://semver.org/):

- **MAJOR** (1.0.0 → 2.0.0): Несовместимые изменения API
- **MINOR** (1.0.0 → 1.1.0): Новая функциональность (обратно совместимая)
- **PATCH** (1.0.0 → 1.0.1): Исправления ошибок

## 📞 Помощь

Если возникли проблемы:
1. Проверьте логи сборки
2. Убедитесь, что все зависимости установлены
3. Проверьте, что тег создан правильно
4. Создайте Issue в репозитории

---

**Удачи с релизом!** 🚀

