# 🚀 Как запустить билд

## Вариант 1: Перезапуск существующего workflow

Если workflow уже был запушен, но упал с ошибкой:

1. Перейдите на GitHub в ваш репозиторий
2. Откройте вкладку **Actions**
3. Найдите последний запуск workflow
4. Нажмите **Re-run all jobs** или **Re-run failed jobs**

## Вариант 2: Создание нового тега (рекомендуется)

Если нужно создать новый релиз:

### Автоматический способ:

```powershell
.\prepare-release.ps1 -Version "1.0.2"
```

Скрипт автоматически:
- Обновит версию в `electron/package.json`
- Создаст коммит
- Создаст тег
- Покажет команды для push

### Ручной способ:

```bash
# 1. Обновите версию в electron/package.json (если нужно)
# Откройте electron/package.json и измените "version": "1.0.2"

# 2. Закоммитьте изменения
git add .
git commit -m "Update version to 1.0.2"

# 3. Создайте тег
git tag -a v1.0.2 -m "Release v1.0.2"

# 4. Отправьте на GitHub
git push origin main
git push origin v1.0.2
```

После push тега GitHub Actions автоматически запустит билд!

## Вариант 3: Ручной запуск workflow (если настроен)

Если у вас есть workflow_dispatch, можно запустить вручную:

1. Перейдите в **Actions**
2. Выберите workflow **Build and Release**
3. Нажмите **Run workflow**
4. Выберите ветку и версию
5. Нажмите **Run workflow**

## Проверка статуса

После запуска:
1. Перейдите в **Actions** на GitHub
2. Найдите запущенный workflow
3. Нажмите на него для просмотра прогресса
4. Дождитесь завершения всех jobs:
   - ✅ build-electron
   - ✅ build-python
   - ✅ create-release

## Результат

После успешного выполнения в **Releases** появятся:
- `Post-Install-Electron-v1.0.2-Setup.exe`
- `Post-Install-Electron-v1.0.2.zip`
- `Post-Install-Python-v1.0.2.zip`

## Локальная сборка (для тестирования)

### Electron:
```bash
cd electron
npm install
npm run build
# Результат в electron/releases/
```

### Python:
```bash
pip install pyinstaller
cd python
pyinstaller --onefile --windowed --name "Post-Install-Python" software_installer.py
# Результат в dist/
```

